(define (baz)
  9)
(define (foo x)
  (* x 10))
(define (bar)
  99999)
(define (quux a b)
  (display " ")
  (+ a b))

(let ((x 10)
      (y 20))
  (foo x)
  (let ((a (bar))
        (b (baz)))
    (quux x a)
    (quux y b))
  (baz))

(let ((x 10)
      (a 20))
  (foo x)
  (let ((x (bar))
        (b (quux x x)))
    (display b)
    (display x)
    (quux x a))
  (quux x a))
